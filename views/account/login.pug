extends ../layout

block content
  .sat-page-header
    h2 Sign In

  .container.py-5
    .sat-auth-card.sat-card-static
      form.sat-form(method='POST')
        input(type='hidden', name='_csrf', value=_csrf)
        .mb-3
          label.form-label(for='email') Email
          input.form-control(type='email', name='email', id='email', placeholder='you@example.com', autofocus, required)
        .mb-3
          label.form-label(for='password') Password
          input.form-control(type='password', name='password', id='password', placeholder='Password', required)
        button.btn.btn-ocean.w-100(type='submit')
          i.bi.bi-box-arrow-in-right.me-1
          | Sign In
      .text-center.mt-3
        a.text-ocean(href='/forgot') Forgot password?
      .text-center.mt-2
        small Don't have an account?
          a.text-ocean.ms-1(href='/signup') Create Account
